# Required for local development
S3_BUCKET_NAME=your-bucket-name

# Optional - AWS region (defaults to us-east-1 when not set)
# AWS_REGION=us-east-1

# Optional - Port for local dev server (defaults to 3000)
# PORT=3000

# Optional - Set to "production" when running in Lambda
# NODE_ENV=development
